# Deteksi Ban AI

Aplikasi ini menggunakan AI untuk mendeteksi kelayakan ban mobil.  
Pengguna tinggal upload foto ban, aplikasi akan menampilkan hasil prediksi.

## Cara Pakai

1. Install virtual environment:
   ```bash
   python -m venv .venv
   .venv\Scripts\activate
   pip install -r requirements.txt
